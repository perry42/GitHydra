{
  "name": "githydra",
  "private": true,
  "version": "0.1.0",
  "description": "GitHydra - a free, open-source visual git client",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/git-core --if-present && npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "start": "electron packages/desktop"
  },
  "license": "GPL-3.0-or-later"
}
